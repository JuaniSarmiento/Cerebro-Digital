---
fecha: 2026-05-28
hora: 01:15 p. m.
fuente: wa-claude (auto-log)
tags: [inbox, sin-revisar, wa-chat, auto-log]
---

# Chat WhatsApp — 2026-05-28 01:15 p. m.

> **Juani** (12:43 p. m.):
> ahora como funcionan los dos modos de IA???

> **Claude** (12:44 p. m.):
> Buena pregunta, te tiro la data corta:
> 
> **Sonnet (por defecto)** — es el modelo que uso siempre. Rápido, barato, suficiente para el 90% de las cosas: charla, búsquedas, ediciones, queries al vault, comandos de sistema, código simple.
> 
> **Opus (bajo demanda)** — más profundo, más lento, más caro. Lo activás vos cuando hace falta: decisiones de arquitectura, debugging difícil, refactors grandes, evaluar trade-offs no triviales, razonamiento multi-paso pesado.
> 
> Cómo se activa: o me decís "modo opus" y lo fuerzo para esa charla, o si detecto que la tarea pinta para Opus te sugiero "esto tiene pinta de necesitar Opus, ¿lo activo?" y vos decidís. Cuando se usa, lanzo un sub-agente con `model: "opus"` para que la tarea pesada corra ahí, y yo te resumo el resultado acá.
> 
> Ahora mismo estamos en **MODO OPUS** activado (lo veo en el system prompt), así que esta sesión ya está corriendo con todo el músculo. ¿Algo puntual que querés atacar con esta capacidad?

> **Juani** (12:46 p. m.):
> fíjate que no usas sonnet, usas una api key analiza el modo rápido entrando al vps así tenes más info

> **Claude** (12:48 p. m.):
> Dale, voy a husmear el VPS a ver cómo está armado realmente el setup.Ahí veo cosas interesantes: `GROQ_API_KEY`, `CLAUDE_EFFORT`, PM2, `AI_AGENT`. Sigo cavando.Encontré wa-claude (el bridge de WhatsApp). Voy a abrir el código para ver cómo está armado el modo rápido vs profundo.Listo, ya lo tengo claro. **Te tenía la peli equivocada** en mi mensaje anterior. La realidad es esta:
> 
> **Modo rápido = DeepSeek V4 Flash** (no Sonnet). API directa a `api.deepseek.com`. Tu wa-claude llama ese endpoint con tu API key (la tenés hardcodeada en `src/claude.js` línea 13, después la sacamos de ahí a env var si querés). Cuesta centavos por mensaje. Tiene 4 tools: `run_command`, `web_search`, `write_file`, `read_file`. Historial de 10 mensajes max. Timeout 30s. System prompt corto que le dice "ahorra tokens, si la tarea es pesada derivá a Opus".
> 
> **Modo opus = Claude Opus vía Agent SDK** (`@anthropic-ai/claude-agent-sdk`, no API REST directa). Es lo que está corriendo ahora mismo en esta sesión — el binario `claude` con `permissionMode: bypassPermissions`. Sin restricciones, acceso completo al VPS, MCPs de DuckDuckGo y Playwright, sin tope de tokens. Timeout 120s. Sesión persistente por chatId (recuerda el `session_id` del SDK y lo reusa con `resume`).
> 
> **Switch**: lo manejás vos con texto exacto. `"modo opus"`, `"activa opus"`, `"usa opus"` → Opus. `"modo rapido"`, `"modo normal"`, `"modo fast"`, `"modo deepseek"` → DeepSeek. Por chat, en memoria (se pierde si reinicia pm2).
> 
> **Detalles que vi de paso que conviene revisar**:
> - La API key de DeepSeek está hardcodeada en el código → mejor `process.env.DEEPSEEK_API_KEY`.
> - Si DeepSeek se queda sin saldo, te avisa con el mensaje literal y te ofrece switch a Opus.
> - El `GROQ_API_KEY` está en el env pero no se usa en wa-claude — ¿es de otro proyecto?
> 
> ¿Querés que limpie lo de la API key hardcodeada o lo dejamos para otro momento?

> **Juani** (12:58 p. m.):
> quiero que dejes ahí comentada la api key de deepseck y pasemos al que el modo rápido sea claude haiku a través de claude code. como lo mismo que opus pero para diario con ahorro de tokens, qué plan me ofreces??

> **Claude** (01:00 p. m.):
> Buena movida — unificás arquitectura y subís calidad en el día a día. Te tiro el plan:
> 
> **Cambios concretos en `src/claude.js`:**
> 
> 1. **API key DeepSeek**: comentar línea 13 (`DEEPSEEK_API_KEY`), `DEEPSEEK_URL`, `DEEPSEEK_MODEL` y el bloque de fetch. Lo dejamos como referencia histórica con un comentario `// LEGACY: modo rápido vía DeepSeek (reemplazado por Haiku vía Agent SDK)`.
> 
> 2. **Reemplazar `askDeepSeek` por `askHaiku`**: mismo patrón que `askOpus` (usa `query()` del SDK), solo cambia el modelo. El SDK acepta `model` en `options` — le pasamos `claude-haiku-4-5`.
> 
> 3. **Borrar el sistema de tools custom**: `TOOLS` array, `executeTool`, `chatHistories`, `getHistory`. Ya no hace falta — el Agent SDK trae Bash, Read, Write, Edit nativos. Sesiones se persisten con `resume` igual que Opus (`haikuSessions` map por chatId).
> 
> 4. **System prompt para Haiku**: corto, mismo tono que el de DeepSeek pero sin las reglas de "tools custom". Mantenemos la regla de oro "responde corto, ahorra tokens" y la derivación a Opus para tareas pesadas (codear, refactorear, multi-paso).
> 
> 5. **MCPs en modo rápido**: te propongo solo `ddg` (DuckDuckGo). Playwright es pesado de arrancar y rara vez lo necesitás para charla diaria — queda exclusivo de Opus.
> 
> 6. **Permisos**: `bypassPermissions` igual que Opus (es tu VPS, no hace falta fricción).
> 
> 7. **Mensajes de switch**: actualizar `"Modo rapido. DeepSeek listo."` → `"Modo rapido. Haiku listo."`.
> 
> 8. **Limpieza de imports**: quitar `execSync` y `fs` si ya no se usan en el archivo (los usaba `executeTool`).
> 
> **Comparativa rápida de costos** (input/output por MTok):
> - DeepSeek V4 Flash: ~$0.07 / $0.27
> - Haiku 4.5: ~$1 / $5
> 
> Para tu volumen (decenas de mensajes/día) la diferencia mensual es centavos de dólar, pero ganás: mejor calidad, mismas tools que Opus, una sola arquitectura para mantener.
> 
> **Lo que necesito confirmar antes de tocar código:**Decime esas tres y arranco con el refactor.

> **Juani** (01:07 p. m.):
> si, que más le agregaría, que tenga si el mcp completo de obsidian porque es importante tener el cerebro y poder agregar y sacar cosa del cerebro y también que funcione con la app de zeep para tener info de mis datos, además de poder ver la hora, el clima y buscar info que quiera en google

> **Claude** (01:09 p. m.):
> Buenísimo, vamos a un Haiku con superpoderes. Antes de armar el plan final, dejame chequear qué tenés instalado y qué MCP de Obsidian conviene.Listo, ya tengo todo el panorama. Te tiro el plan completo actualizado:
> 
> **Modo rápido = Claude Haiku 4.5 vía Agent SDK con estas capacidades:**
> 
> ### MCPs que le sumamos
> - **`obsidian-mcp`** (de Stevens) — vault completo. No requiere plugin Local REST API porque corre contra filesystem directo. Te da: list notes, read note, search by content/tag, create note, append, delete. Apunta a `/home/claude-bot/proyectos/Cerebro-Digital/CerebroDigital/`.
> - **`ddg`** — búsqueda web (cubre lo que necesitarías de Google sin API key paga).
> - *(Opus seguirá teniendo además `playwright` para navegación pesada.)*
> 
> ### Lo que NO necesita MCP (lo cubre el SDK nativo + Bash)
> - **Zepp**: `zepp-brief brief` y `zepp-brief sleep` ya están como CLI global. Lo llama por Bash. Si querés más detalle, también puede tirar `python3 /home/claude-bot/proyectos/zepp-health-cli/zepp_health.py <subcomando>`.
> - **Hora**: ya se inyecta cada turno en el system prompt (`Hoy es jueves, 28 de mayo...`). Si quiere hora exacta, Bash `date`.
> - **Clima**: `curl -s "wttr.in/Mendoza?format=%C+%t+%w&lang=es"` — sin API key, sin dependencias. Probado, devuelve "Parcialmente nublado +14°C ↑5km/h".
> - **Vault avanzado** (si el MCP falla): Read/Write/Edit/Glob/Grep nativos del SDK contra el filesystem.
> 
> ### Cambios concretos en `src/claude.js`
> 1. Comentar bloque DeepSeek (líneas 13-15 + fetch + `TOOLS` + `executeTool` + `chatHistories`) con header `// LEGACY: DeepSeek V4 Flash — reemplazado por Haiku 4.5 vía Agent SDK`.
> 2. Crear `askHaiku()` clonando `askOpus()` con: `model: 'claude-haiku-4-5'`, MCPs `{ ddg, obsidian }`, `haikuSessions` map para resume.
> 3. System prompt corto para Haiku (regla de oro: responder corto, derivar pesado a Opus, info de Juani).
> 4. Mensaje de switch: `"Modo rapido. Haiku listo."`.
> 5. Limpiar imports muertos (`execSync`, `fs` si quedaron sin uso).
> 
> ### Estimación de costo
> Haiku 4.5 a tu volumen (~50-100 msgs/día) ronda los **USD 1-2/mes**. Cero drama.
> 
> ### Riesgos / cosas a tener en cuenta
> - `obsidian-mcp` lo instala `npx` la primera vez (latencia inicial). Después queda cacheado.
> - Si querés escritura segura al vault, el MCP de Obsidian respeta el directorio que le pasás como argumento — no puede salir de ahí. Más seguro que dejar Bash libre.
> - Haiku no es Sonnet — para charla y comandos va perfecto, pero si le pedís razonamiento complejo en modo rápido te va a quedar corto. Para eso está la derivación a Opus.
> 
> **Pregunta clave antes de tocar:** ¿confirmás Haiku 4.5 + ddg + obsidian-mcp (Stevens), o querés también que el modo rápido tenga Playwright (te navega webs pero arranca más lento, +1-2s la primera vez)?

> **Juani** (01:13 p. m.):
> pero fíjate cómo funciona el modo opus, como busca en internet, cómo trabaja con obsidian y replícalo en el modo haiku

> **Claude** (01:14 p. m.):
> Tenés razón, dejame revisar exactamente cómo Opus accede a todo eso. Voy a leer el código con detalle.Mirá qué interesante — **Opus NO usa MCP de Obsidian**. Trabaja con el vault usando las tools nativas del Agent SDK (Read, Write, Edit, Glob, Grep, Bash). Solo le dice el path en el system prompt y listo. Para internet sí tiene dos MCPs: `ddg` (DuckDuckGo) y `playwright` (navegación headless).
> 
> Entonces el plan se simplifica mucho — replicar **exactamente** la config de Opus, solo cambiando el modelo. Esto te queda:
> 
> ### Plan final corregido
> 
> **`askHaiku()` = clon de `askOpus()` con dos cambios:**
> 
> ```javascript
> async function askHaiku(chatId, userMessage, imageData = null) {
>   const resumeId = haikuSessions.get(chatId);
> 
>   const haikuPrompt = `Sos Jarvis, asistente personal de Juani... [mismo bloque que Opus]
> 
> MODO RAPIDO (Haiku 4.5) — Mismas capacidades que Opus pero optimizado para charla diaria:
> - Bash sin restricciones (zepp-brief, gcal, gmail, spotify, gh, curl wttr.in/Mendoza, date)
> - Read/Write/Edit en el vault Obsidian: /home/claude-bot/proyectos/Cerebro-Digital/CerebroDigital/
> - Busqueda web via ddg MCP
> - Navegacion web via playwright MCP
> - Proyectos: /home/claude-bot/proyectos/
> 
> REGLA DE ORO: respondé corto, ahorrá tokens. Si la tarea es pesada (refactors grandes, debugging multi-paso, arquitectura), decile "Esto conviene en modo Opus, decime 'modo opus'."
> 
> Hoy es ${dateStr}.`;
> 
>   const result = query({
>     prompt: promptInput,
>     options: {
>       resume: resumeId,
>       systemPrompt: haikuPrompt,
>       cwd: WORKDIR,
>       permissionMode: 'bypassPermissions',
>       model: 'claude-haiku-4-5',          // ← ÚNICA diferencia técnica con Opus
>       mcpServers: {
>         ddg: { command: 'npx', args: ['-y', '@anthropic-ai/duckduckgo-mcp-server'] },
>         playwright: { command: 'npx', args: ['@playwright/mcp', '--headless', '--browser', 'chromium'] },
>       },
>     },
>   });
>   // ...resto idéntico a askOpus
> }
> ```
> 
> ### Lo que esto implica
> - **Vault Obsidian**: mismo acceso que Opus — lee/escribe/busca con tools nativas. Sin MCP extra. Path en el prompt.
> - **Internet**: ddg + playwright iguales. Puede googlear, scrapear, navegar.
> - **Zepp**: Bash con `zepp-brief brief` o `zepp-brief sleep`. Igual que Opus.
> - **Hora/clima**: inyección de fecha en system prompt + `curl wttr.in/Mendoza` por Bash. Igual que Opus.
> - **Sesiones persistentes**: `haikuSessions` Map por chatId, separado del de Opus para no mezclar contextos.
> 
> ### Cambios completos al archivo
> 1. Comentar todo el bloque DeepSeek (constantes + fetch + TOOLS + executeTool + chatHistories + askDeepSeek) con header `// LEGACY: DeepSeek V4 Flash — reemplazado por Haiku 4.5 vía Agent SDK`.
> 2. Agregar `const haikuSessions = new Map();` arriba de `opusSessions`.
> 3. Implementar `askHaiku()` (~30 líneas, clon de Opus).
> 4. En `askClaude()` cambiar `await askDeepSeek(...)` por `await askHaiku(...)`.
> 5. Actualizar mensaje switch: `"Modo rapido. Haiku listo."`.
> 6. Limpiar imports muertos.
> 
> ### Costo estimado
> USD 1-3/mes a tu volumen. La diferencia con DeepSeek es marginal y ganás consistencia total con Opus.
> 
> ¿Te tiro el refactor ahora?
